const Shimmer = ({ h, w, mh, mw }) => {
  return (
    <div
      style={{
        height: `${h}px`,
        width: `${w}px`,
      }}
      className='shimmer-icon'></div>
  );
};

export default Shimmer;
